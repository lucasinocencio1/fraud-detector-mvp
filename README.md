# Fraud Detector MVP

Fraud detection stack built with FastAPI and XGBoost. The project serves a probabilistic model over HTTP and ships with a production-oriented data pipeline with ramdom data, testing harness, and deployment tooling.